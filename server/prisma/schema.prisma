generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Domain models are intentionally omitted in Milestone 1.
// Milestone 2+ will introduce User, Company, Trip, and related entities.
